command:
  $$(command) as @s[$(id)=$(uuid)]

expected:
  (source_file
    (command
      (command_name_macro)
      (macro_interpolation)
      (argument_common)
      (selector
        (selector_variable)
        (selector_arguments
          argument: (selector_argument
            (selector_key
              (macro_interpolation))
            (macro_interpolation))))))
