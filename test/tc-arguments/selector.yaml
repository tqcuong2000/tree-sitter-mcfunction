command:
  tellraw @a[name=name] "hello"
  tag @n add test

expected:
  (source_file
    (command
      (command_name)
      (selector
        (selector_variable)
        (selector_arguments
          argument: (selector_argument
            (selector_key)
            (selector_value))))
      (string))
    (command
      (command_name)
      (selector
        (selector_variable))
      (argument_common)
      (argument_common)))
