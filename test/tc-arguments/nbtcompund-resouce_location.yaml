command:
  function flags:lib/test/fol_1 {1:"id", _a:"test", b:hello}

expected:
  (source_file
    (command
      (command_name)
      (resource_location)
      (nbt_compound
        (nbt_key)
        (string)
        (nbt_key)
        (string)
        (nbt_key)
        (unquoted_string))))
